<div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8">
  <h1 class="text-3xl font-bold mb-4">Информация о пользователе</h1>

  <div class="bg-white shadow overflow-hidden sm:rounded-lg mb-8">
    <div class="px-4 py-5 sm:px-6">
      <dl class="grid grid-cols-1 gap-x-4 gap-y-8 sm:grid-cols-2">
        <div class="sm:col-span-1">
          <dt class="text-sm font-medium text-gray-500">Имя</dt>
          <dd class="mt-1 text-lg text-gray-900"><%= @user.first_name %></dd>
        </div>
        <div class="sm:col-span-1">
          <dt class="text-sm font-medium text-gray-500">Фамилия</dt>
          <dd class="mt-1 text-lg text-gray-900"><%= @user.last_name %></dd>
        </div>
        <div class="sm:col-span-1">
          <dt class="text-sm font-medium text-gray-500">Email</dt>
          <dd class="mt-1 text-lg text-gray-900"><%= @user.email %></dd>
        </div>
      </dl>
    </div>
  </div>

  <div class="flex items-center justify-between">
    <%= link_to 'Редактировать', edit_user_path(@user), class: "py-2 px-4 border border-transparent text-sm font-medium rounded-md text-white bg-indigo-600 hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500" %>
    <%= link_to 'Вернуться к списку', users_path, class: "py-2 px-4 border border-transparent text-sm font-medium rounded-md text-indigo-600 bg-white hover:bg-indigo-50" %>
  </div>
</div>
